<section class="mx-auto max-w-6xl px-4 py-8">
  <div class="mb-6 rounded-xl border border-slate-200 bg-white p-4 md:p-6 shadow-lg dark:border-slate-700 dark:bg-slate-900" data-controller="tabs" data-tabs-default-value="<%= params[:tab].presence || 'overview' %>">
    <div class="flex flex-col gap-4">
      <!-- Header -->
      <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">
        <h1 class="text-2xl sm:text-3xl font-bold bg-gradient-to-r from-sky-600 via-indigo-500 to-cyan-500 bg-clip-text text-transparent dark:from-sky-400 dark:via-indigo-400 dark:to-cyan-400 animate-gradient">Ops Admin</h1>
        
        <!-- Quick Links -->
        <div class="flex flex-wrap gap-2">
          <a href="<%= ops_ownerships_path %>" class="rounded-lg border border-slate-200 bg-slate-50 px-3 py-1.5 text-sm text-slate-700 hover:bg-slate-100 hover:border-slate-300 dark:border-slate-700 dark:bg-slate-800 dark:text-slate-300 dark:hover:bg-slate-700">Users & Ownership</a>
          <a href="/ops/profiles/<%= Profile.order(:login).limit(1).pluck(:login).first %>" class="rounded-lg border border-slate-200 bg-slate-50 px-3 py-1.5 text-sm text-slate-700 hover:bg-slate-100 hover:border-slate-300 dark:border-slate-700 dark:bg-slate-800 dark:text-slate-300 dark:hover:bg-slate-700">Example Profile</a>
          <a href="<%= ops_motifs_path %>" class="rounded-lg border border-slate-200 bg-slate-50 px-3 py-1.5 text-sm text-slate-700 hover:bg-slate-100 hover:border-slate-300 dark:border-slate-700 dark:bg-slate-800 dark:text-slate-300 dark:hover:bg-slate-700">Motifs</a>
        </div>
      </div>

      <!-- Tab Navigation -->
      <div class="sticky top-[60px] z-40 -mx-4 md:-mx-6 border-b border-slate-200 dark:border-slate-700 bg-white/95 backdrop-blur-sm dark:bg-slate-900/95">
        <nav class="flex flex-wrap justify-start gap-2 pb-4 px-2 overflow-x-auto" aria-label="Ops Admin Tabs">
          <button data-tabs-target="tab" data-tab-name="overview" data-action="click->tabs#change" class="flex items-center gap-2 whitespace-nowrap rounded-lg px-4 py-2 text-sm md:text-base font-medium transition border-transparent text-slate-500 hover:bg-slate-100 hover:text-slate-700 dark:text-slate-400 dark:hover:bg-slate-800 dark:hover:text-slate-300 data-[active]:bg-indigo-600 data-[active]:text-white">
            <span>Overview</span>
          </button>
          <button data-tabs-target="tab" data-tab-name="profiles" data-action="click->tabs#change" class="flex items-center gap-2 whitespace-nowrap rounded-lg px-4 py-2 text-sm md:text-base font-medium transition border-transparent text-slate-500 hover:bg-slate-100 hover:text-slate-700 dark:text-slate-400 dark:hover:bg-slate-800 dark:hover:text-slate-300 data-[active]:bg-indigo-600 data-[active]:text-white">
            <span>Profiles</span>
          </button>
          <button data-tabs-target="tab" data-tab-name="ai" data-action="click->tabs#change" class="flex items-center gap-2 whitespace-nowrap rounded-lg px-4 py-2 text-sm md:text-base font-medium transition border-transparent text-slate-500 hover:bg-slate-100 hover:text-slate-700 dark:text-slate-400 dark:hover:bg-slate-800 dark:hover:text-slate-300 data-[active]:bg-indigo-600 data-[active]:text-white">
            <span>AI</span>
          </button>
          <button data-tabs-target="tab" data-tab-name="pipeline" data-action="click->tabs#change" class="flex items-center gap-2 whitespace-nowrap rounded-lg px-4 py-2 text-sm md:text-base font-medium transition border-transparent text-slate-500 hover:bg-slate-100 hover:text-slate-700 dark:text-slate-400 dark:hover:bg-slate-800 dark:hover:text-slate-300 data-[active]:bg-indigo-600 data-[active]:text-white">
            <span>Pipeline</span>
          </button>
          <button data-tabs-target="tab" data-tab-name="access" data-action="click->tabs#change" class="flex items-center gap-2 whitespace-nowrap rounded-lg px-4 py-2 text-sm md:text-base font-medium transition border-transparent text-slate-500 hover:bg-slate-100 hover:text-slate-700 dark:text-slate-400 dark:hover:bg-slate-800 dark:hover:text-slate-300 data-[active]:bg-indigo-600 data-[active]:text-white">
            <span>Access</span>
          </button>
          <button data-tabs-target="tab" data-tab-name="system" data-action="click->tabs#change" class="flex items-center gap-2 whitespace-nowrap rounded-lg px-4 py-2 text-sm md:text-base font-medium transition border-transparent text-slate-500 hover:bg-slate-100 hover:text-slate-700 dark:text-slate-400 dark:hover:bg-slate-800 dark:hover:text-slate-300 data-[active]:bg-indigo-600 data-[active]:text-white">
            <span>System</span>
          </button>
          <button data-tabs-target="tab" data-tab-name="jobs" data-action="click->tabs#change" class="flex items-center gap-2 whitespace-nowrap rounded-lg px-4 py-2 text-sm md:text-base font-medium transition border-transparent text-slate-500 hover:bg-slate-100 hover:text-slate-700 dark:text-slate-400 dark:hover:bg-slate-800 dark:hover:text-slate-300 data-[active]:bg-indigo-600 data-[active]:text-white">
            <span>Jobs</span>
          </button>
        </nav>
      </div>
    </div>

    <!-- Tab Panels -->
    <div class="mt-6 space-y-6">
      <!-- Overview Tab -->
      <div data-tabs-target="panel" data-tab-panel="overview">
        <!-- Regenerate All Callout -->
        <div class="rounded-lg border border-amber-300 bg-amber-50 p-4 shadow-sm dark:border-amber-700 dark:bg-amber-900/20">
          <div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
            <div>
              <h2 class="text-lg font-semibold text-amber-900 dark:text-amber-200">Re-roll All Profiles</h2>
              <p class="text-sm text-amber-800 dark:text-amber-300">Use after deploying new features or layout updates. This re-runs the profile pipeline for everyone so cards and data match the latest version. The pipeline syncs GitHub data, refreshes traits, and captures screenshots; AI artwork is handled separately. The main Re-roll behaves like a fresh submission.</p>
              <ul class="mt-2 list-disc pl-5 text-xs text-amber-800 dark:text-amber-300">
                <li>Refresh GitHub data and sync the profile</li>
                <li>Rebuild profile info and traits (AI text if enabled; heuristic fallback otherwise)</li>
                <li>Capture card screenshots (OG, card, simple, banner) and queue social variants</li>
                <li>Record/optimize assets for use across the site</li>
              </ul>
            </div>
            <div class="flex flex-wrap gap-2">
              <form action="<%= ops_bulk_retry_all_path %>" method="post" data-controller="submit-form" onsubmit="return confirm('Apply latest version for ALL profiles?');">
                <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
                <button type="submit" class="<%= ui_primary_button_class %>" data-submit-form-target="button">
                  <span data-submit-form-target="spinner" class="hidden"><%= ui_spinner_icon %></span>
                  <span data-submit-form-target="text">Re-roll all profiles</span>
                </button>
              </form>
            </div>
          </div>
        </div>

        <!-- Profile Status Summary -->
        <div class="rounded border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-800">
          <h2 class="text-lg font-semibold mb-3 flex items-center gap-2">
            Profile Status
            <span class="inline-flex items-center" title="Partial = succeeded via fallback (e.g., AI traits or images fell back to heuristics/screenshots). Failure = last pipeline run failed at some stage. Queued = run enqueued.">
              <svg class="h-4 w-4 text-slate-500" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
                <path d="M18 10A8 8 0 112 10a8 8 0 0116 0zM9 8a1 1 0 112 0v5a1 1 0 11-2 0V8zm1-4a1.5 1.5 0 100 3 1.5 1.5 0 000-3z"/>
              </svg>
            </span>
          </h2>
          <div class="grid grid-cols-2 gap-3 sm:grid-cols-5">
            <div class="rounded border border-slate-200 p-3 text-center dark:border-slate-700">
              <div class="text-xs uppercase tracking-wide text-emerald-600 dark:text-emerald-400">Success</div>
              <div class="text-xl font-bold text-slate-900 dark:text-slate-100"><%= @status_counts[:success] %></div>
            </div>
            <div class="rounded border border-slate-200 p-3 text-center dark:border-slate-700">
              <div class="text-xs uppercase tracking-wide text-amber-600 dark:text-amber-400">Partial</div>
              <div class="text-xl font-bold text-slate-900 dark:text-slate-100"><%= @status_counts[:partial_success] %></div>
            </div>
            <div class="rounded border border-slate-200 p-3 text-center dark:border-slate-700">
              <div class="text-xs uppercase tracking-wide text-red-600 dark:text-red-400">Failure</div>
              <div class="text-xl font-bold text-slate-900 dark:text-slate-100"><%= @status_counts[:failure] %></div>
            </div>
            <div class="rounded border border-slate-200 p-3 text-center dark:border-slate-700">
              <div class="text-xs uppercase tracking-wide text-indigo-600 dark:text-indigo-400">Queued</div>
              <div class="text-xl font-bold text-slate-900 dark:text-slate-100"><%= @status_counts[:queued] %></div>
            </div>
            <div class="rounded border border-slate-200 p-3 text-center dark:border-slate-700">
              <div class="text-xs uppercase tracking-wide text-slate-500 dark:text-slate-400">Unknown</div>
              <div class="text-xl font-bold text-slate-900 dark:text-slate-100"><%= @status_counts[:unknown] %></div>
            </div>
          </div>
          <p class="mt-3 text-xs text-slate-600 dark:text-slate-400">Status reflects the last pipeline run per profile.</p>
        </div>
      </div>

      <!-- Profiles Tab -->
      <div data-tabs-target="panel" data-tab-panel="profiles" class="hidden space-y-6">
        <!-- Recent Successful Profiles -->
        <div class="rounded border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-800">
          <h2 class="text-lg font-semibold mb-3">Recent Successful Profiles</h2>
          <% if @recent_success_profiles.present? %>
            <div class="overflow-x-auto">
              <table class="min-w-full text-sm">
                <thead>
                  <tr class="text-left text-slate-500 dark:text-slate-400 border-b border-slate-200 dark:border-slate-700">
                    <th class="py-2 pr-4">Profile</th>
                    <th class="py-2 pr-4">Status</th>
                    <th class="py-2 pr-4">Last Synced</th>
                    <th class="py-2 pr-4">Last AI Regen</th>
                    <th class="py-2">Actions</th>
                  </tr>
                </thead>
                <tbody>
                  <% @recent_success_profiles.each do |p| %>
                    <tr class="border-b border-slate-100 dark:border-slate-800 align-top">
                      <td class="py-2 pr-4">
                        <a href="<%= profile_path(username: p.login) %>" class="text-blue-600 dark:text-blue-400">@<%= p.login %></a>
                        <span class="text-slate-400">·</span>
                        <a href="<%= ops_profile_admin_path(username: p.login) %>" class="text-slate-600 underline dark:text-slate-300">Ops</a>
                      </td>
                      <td class="py-2 pr-4">
                        <% status = p.last_pipeline_status %>
                        <% cls = case status
                                 when 'success' then 'bg-emerald-100 text-emerald-800 dark:bg-emerald-900/30 dark:text-emerald-300'
                                 when 'partial_success' then 'bg-amber-100 text-amber-800 dark:bg-amber-900/30 dark:text-amber-300'
                                 else 'bg-slate-100 text-slate-700 dark:bg-slate-800 dark:text-slate-300'
                               end %>
                        <% tip = case status
                                 when 'success' then 'Full success'
                                 when 'partial_success' then 'Partial: succeeded via fallback (e.g., heuristics/screenshots)'
                                 else nil
                               end %>
                        <span class="rounded px-2 py-0.5 text-xs font-semibold <%= cls %>" <%= "title=\"#{h tip}\"" if tip.present? %>><%= status %></span>
                      </td>
                      <td class="py-2 pr-4"><%= p.last_synced_at&.strftime('%Y-%m-%d %H:%M') || '—' %></td>
                      <td class="py-2 pr-4"><%= p.last_ai_regenerated_at&.strftime('%Y-%m-%d %H:%M') || '—' %></td>
                      <td class="py-2">
                        <div class="flex flex-wrap gap-2">
                          <%= button_to ops_retry_profile_path(username: p.login),
                                method: :post,
                                form: { data: { controller: "submit-form" } },
                                class: "#{ui_secondary_button_class} px-4",
                                data: { submit_form_target: "button" } do %>
                            <span data-submit-form-target="spinner" class="hidden"><%= ui_spinner_icon(classes: "h-3.5 w-3.5") %></span>
                            <span data-submit-form-target="text">Re-roll profile</span>
                          <% end %>
                        </div>
                      </td>
                    </tr>
                  <% end %>
                </tbody>
              </table>
            </div>
          <% else %>
            <p class="text-slate-600 dark:text-slate-300">No successful runs recorded yet.</p>
          <% end %>
        </div>

        <div class="rounded border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-800">
          <h2 class="text-lg font-semibold mb-1">Asset Refresh (Non-destructive)</h2>
          <p class="mb-3 text-sm text-slate-600 dark:text-slate-300">Regenerate missing screenshots (or specific variants) without touching profile data. Useful when new asset kinds launch.</p>
          <form action="<%= ops_bulk_refresh_assets_path %>" method="post" class="space-y-3" data-controller="submit-form">
            <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
            <div class="grid gap-4 sm:grid-cols-2">
              <div class="space-y-2">
                <label class="text-xs font-semibold uppercase tracking-wide text-slate-500 dark:text-slate-400">Target profiles (optional)</label>
                <textarea name="logins" rows="4" class="<%= ui_textarea_class %>" placeholder="loftwah&#10;lindsey"></textarea>
                <p class="text-xs text-slate-500 dark:text-slate-400">Leave blank to process everyone. Separate usernames with spaces, commas, or new lines.</p>
              </div>
              <div class="space-y-2">
                <label class="text-xs font-semibold uppercase tracking-wide text-slate-500 dark:text-slate-400">Asset variants</label>
                <input type="text" name="variants" class="<%= ui_input_class %>" placeholder="og card simple (defaults to all)">
                <label class="inline-flex items-center gap-2 text-sm text-slate-600 dark:text-slate-300">
                  <input type="checkbox" name="only_missing" value="1" checked class="rounded border-slate-300 text-emerald-600 focus:ring-emerald-500">
                  Only refresh missing variants
                </label>
                <p class="text-xs text-slate-500 dark:text-slate-400">Uncheck to force-regenerate the listed variants even if they already exist.</p>
              </div>
            </div>
            <button type="submit" class="<%= ui_primary_button_class %>" data-submit-form-target="button" onclick="return confirm('Queue non-destructive asset refresh jobs?');">
              <span data-submit-form-target="spinner" class="hidden"><%= ui_spinner_icon %></span>
              <span data-submit-form-target="text">Queue asset refresh</span>
            </button>
          </form>
        </div>

        <div class="rounded border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-800">
          <h2 class="text-lg font-semibold mb-1">GitHub Refresh (Non-destructive)</h2>
          <p class="mb-3 text-sm text-slate-600 dark:text-slate-300">Queue a lightweight GitHub sync that preserves user-edited fields (name, bio, etc.) or refreshes avatars only.</p>
          <form action="<%= ops_bulk_refresh_github_path %>" method="post" class="space-y-3" data-controller="submit-form">
            <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
            <div class="grid gap-4 sm:grid-cols-2">
              <div class="space-y-2">
                <label class="text-xs font-semibold uppercase tracking-wide text-slate-500 dark:text-slate-400">Target profiles (optional)</label>
                <textarea name="logins" rows="4" class="<%= ui_textarea_class %>" placeholder="loftwah&#10;lindsey"></textarea>
                <p class="text-xs text-slate-500 dark:text-slate-400">Leave blank to refresh everyone. Separate usernames with spaces, commas, or line breaks.</p>
              </div>
              <div class="space-y-2">
                <label class="text-xs font-semibold uppercase tracking-wide text-slate-500 dark:text-slate-400">Mode</label>
                <select name="mode" class="<%= ui_select_class %>">
                  <option value="github">GitHub data (preserve avatar + text fields)</option>
                  <option value="avatar">Avatar only</option>
                </select>
                <p class="text-xs text-slate-500 dark:text-slate-400">Data mode syncs repos/orgs/etc. but keeps manual profile edits. Avatar mode only updates avatar assets.</p>
              </div>
            </div>
            <button type="submit" class="<%= ui_primary_button_class %>" data-submit-form-target="button">
              <span data-submit-form-target="spinner" class="hidden"><%= ui_spinner_icon %></span>
              <span data-submit-form-target="text">Queue GitHub refresh</span>
            </button>
          </form>
        </div>

        <!-- Failed Profiles -->
        <div class="rounded border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-800">
          <h2 class="text-lg font-semibold mb-1">Failed Profiles</h2>
          <p class="mb-3 text-sm text-slate-600 dark:text-slate-300">Regeneration recaptures screenshots and updates text. AI artwork is managed outside of the pipeline.</p>
          <p class="-mt-2 mb-3 text-xs text-red-700 dark:text-red-400">"Delete profile (permanent)" removes the profile and all related data. This cannot be undone.</p>
          <div class="mb-3 grid gap-3 sm:grid-cols-2">
            <div class="rounded border border-slate-200 p-3 dark:border-slate-700">
              <h3 class="text-sm font-semibold mb-2">Re-roll (Selected)</h3>
              <form action="<%= ops_bulk_retry_path %>" method="post" class="space-y-2" data-controller="submit-form">
                <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
                <label class="text-xs font-semibold uppercase tracking-wide text-slate-500 dark:text-slate-400">Select profiles</label>
                <select name="logins[]" multiple size="8" class="<%= "#{ui_select_class} h-auto min-h-[12rem]" %>">
                  <% Profile.order(:login).limit(500).pluck(:login).each do |login| %>
                    <option value="<%= login %>">@<%= login %></option>
                  <% end %>
                </select>
                <button type="submit" class="<%= ui_primary_button_class %> w-full" data-submit-form-target="button">
                  <span data-submit-form-target="spinner" class="hidden"><%= ui_spinner_icon %></span>
                  <span data-submit-form-target="text">Re-roll selected profiles</span>
                </button>
              </form>
            </div>
            <div class="rounded border border-slate-200 p-3 dark:border-slate-700">
              <h3 class="text-sm font-semibold mb-2">Advanced Re-roll (Single Profile)</h3>
              <form action="<%= ops_retry_profile_path(username: '') %>" method="post" data-controller="submit-form" onsubmit="this.action=this.action.replace(/\/$/, '') + '/' + encodeURIComponent(this.querySelector('[name=login]').value) + '/retry';">
                <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
                <div class="mb-2 space-y-1.5">
                  <label class="text-xs font-semibold uppercase tracking-wide text-slate-500 dark:text-slate-400">GitHub username</label>
                  <input type="text" name="login" class="<%= ui_input_class %>" placeholder="loftwah" required>
                </div>
                <p class="text-xs text-slate-500 dark:text-slate-400 mb-2">Runs the full pipeline as if freshly submitted. More granular controls coming soon.</p>
                <button type="submit" class="<%= ui_primary_button_class %> w-full" data-submit-form-target="button">
                  <span data-submit-form-target="spinner" class="hidden"><%= ui_spinner_icon %></span>
                  <span data-submit-form-target="text">Re-roll profile</span>
                </button>
              </form>
            </div>
          </div>

          <div class="mb-3 flex items-center gap-2">
            <form action="<%= ops_bulk_retry_all_path %>" method="post" data-controller="submit-form">
              <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
              <button type="submit" class="<%= ui_primary_button_class %>" data-submit-form-target="button">
                <span data-submit-form-target="spinner" class="hidden"><%= ui_spinner_icon %></span>
                <span data-submit-form-target="text">Re-roll all profiles</span>
              </button>
            </form>
          </div>
          <% if @failed_profiles.present? %>
            <div class="overflow-x-auto">
              <table class="min-w-full text-sm">
                <thead>
                  <tr class="text-left text-slate-500 dark:text-slate-400 border-b border-slate-200 dark:border-slate-700">
                    <th class="py-2 pr-4">Profile</th>
                    <th class="py-2 pr-4">Last Error</th>
                    <th class="py-2 pr-4">Updated</th>
                    <th class="py-2">Actions</th>
                  </tr>
                </thead>
                <tbody>
                  <% @failed_profiles.each do |p| %>
                    <tr class="border-b border-slate-100 dark:border-slate-800 align-top">
                      <td class="py-2 pr-4">
                        <a href="<%= profile_path(username: p.login) %>" class="text-blue-600 dark:text-blue-400">@<%= p.login %></a>
                        <span class="text-slate-400">·</span>
                        <a href="<%= ops_profile_admin_path(username: p.login) %>" class="text-slate-600 underline dark:text-slate-300">Ops</a>
                      </td>
                      <td class="py-2 pr-4">
                        <pre class="whitespace-pre-wrap text-xs text-red-600 dark:text-red-400"><%= (p.last_pipeline_error || "(none)").to_s.truncate(160) %></pre>
                        <% if @failed_events_by_profile_id[p.id] %>
                          <% recent = @failed_events_by_profile_id[p.id].select { |e| e.stage.start_with?("screenshot_") }.first(3) %>
                          <% if recent.any? %>
                            <div class="mt-1 text-[11px] text-slate-500 dark:text-slate-400">
                              <% recent.each do |e| %>
                                <div>
                                  <span class="font-mono"><%= e.stage %></span>
                                  <span>—</span>
                                  <span class="uppercase <%= e.status == 'failed' ? 'text-red-600 dark:text-red-400' : 'text-slate-600 dark:text-slate-300' %>"><%= e.status %></span>
                                  <% if e.message.present? %>
                                    <span class="ml-1">(<%= e.message.to_s.truncate(100) %>)</span>
                                  <% end %>
                                </div>
                              <% end %>
                            </div>
                          <% end %>
                        <% end %>
                      </td>
                      <td class="py-2 pr-4"><%= p.updated_at.strftime('%Y-%m-%d %H:%M') %></td>
                      <td class="py-2">
                        <div class="flex gap-2">
                          <%= button_to "Retry — No Images", ops_retry_profile_path(username: p.login), method: :post, class: "rounded bg-slate-900 px-3 py-1 text-white dark:bg-slate-100 dark:text-slate-900" %>
                          <%= button_to "Delete profile (permanent)", ops_destroy_profile_path(username: p.login, back_to: "#failed-profiles"), method: :delete, form: { data: { turbo_confirm: "Delete @#{p.login}? This permanently removes the profile and all related data. This cannot be undone." } }, class: "rounded bg-red-600 px-3 py-1 text-white hover:bg-red-700" %>
                        </div>
                      </td>
                    </tr>
                  <% end %>
                </tbody>
              </table>
            </div>
          <% else %>
            <p class="text-slate-600 dark:text-slate-300">No failed profiles found.</p>
          <% end %>
        </div>

        <!-- Data Issues -->
        <div class="rounded border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-800">
          <h2 class="text-lg font-semibold mb-3">Data Issues (Successful but incomplete)</h2>
          <% if @data_issues_profiles.present? %>
            <div class="overflow-x-auto">
              <table class="min-w-full text-sm">
                <thead>
                  <tr class="text-left text-slate-500 dark:text-slate-400 border-b border-slate-200 dark:border-slate-700">
                    <th class="py-2 pr-4">Profile</th>
                    <th class="py-2 pr-4">Missing</th>
                    <th class="py-2 pr-4">Last Synced</th>
                    <th class="py-2">Actions</th>
                  </tr>
                </thead>
                <tbody>
                  <% @data_issues_profiles.each do |p| %>
                    <% missing = [] %>
                    <% missing << 'card' unless p.profile_card.present? %>
                    <% missing << 'languages' unless p.profile_languages.exists? %>
                    <% missing << 'repositories' unless p.profile_repositories.exists? %>
                    <tr class="border-b border-slate-100 dark:border-slate-800 align-top">
                      <td class="py-2 pr-4">
                        <a href="<%= profile_path(username: p.login) %>" class="text-blue-600 dark:text-blue-400">@<%= p.login %></a>
                        <span class="text-slate-400">·</span>
                        <a href="<%= ops_profile_admin_path(username: p.login) %>" class="text-slate-600 underline dark:text-slate-300">Ops</a>
                      </td>
                      <td class="py-2 pr-4">
                        <span class="rounded bg-amber-100 px-2 py-0.5 text-amber-800 dark:bg-amber-900/30 dark:text-amber-300"><%= missing.join(', ') %></span>
                      </td>
                      <td class="py-2 pr-4"><%= p.last_synced_at&.strftime('%Y-%m-%d %H:%M') || '—' %></td>
                      <td class="py-2">
                        <div class="flex gap-2">
                          <%= button_to "Retry (no images)", ops_retry_profile_path(username: p.login), method: :post, class: "rounded bg-slate-900 px-3 py-1 text-white dark:bg-slate-100 dark:text-slate-900" %>
                        </div>
                      </td>
                    </tr>
                  <% end %>
                </tbody>
              </table>
            </div>
          <% else %>
            <p class="text-slate-600 dark:text-slate-300">No data issues detected.</p>
          <% end %>
        </div>
      </div>

      <!-- AI Tab -->
      <div data-tabs-target="panel" data-tab-panel="ai" class="hidden space-y-6">
        <!-- AI Capabilities -->
        <div class="rounded border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-800">
          <h2 class="mb-3 text-lg font-semibold text-slate-900 dark:text-slate-100">AI Capabilities</h2>
          <div class="text-sm text-slate-600 dark:text-slate-300 mb-3">
            Provider: <code><%= @ai_caps[:provider] || 'n/a' %></code>
            <span class="mx-2">·</span>
            Model: <code><%= @ai_caps[:model] || 'n/a' %></code>
          </div>
          <div class="grid gap-2 sm:grid-cols-2 lg:grid-cols-4">
            <% [
              ['Image Generation', :image_generation],
              ['Image Descriptions', :image_descriptions],
              ['Text Output', :text_output],
              ['Structured Output', :structured_output]
            ].each do |label, key| %>
              <% on = @ai_caps[key] %>
              <div class="rounded border border-slate-200 p-3 dark:border-slate-700">
                <div class="flex items-center justify-between">
                  <div class="text-slate-800 dark:text-slate-200 text-sm font-medium"><%= label %></div>
                  <% if on %>
                    <span class="rounded bg-emerald-100 px-2 py-0.5 text-xs font-semibold text-emerald-800 dark:bg-emerald-900/30 dark:text-emerald-300">On</span>
                  <% else %>
                    <span class="rounded bg-slate-100 px-2 py-0.5 text-xs font-semibold text-slate-700 dark:bg-slate-800 dark:text-slate-300">Off</span>
                  <% end %>
                </div>
              </div>
            <% end %>
          </div>
          <p class="mt-2 text-xs text-slate-500 dark:text-slate-400">Image modules default Off to control costs with Gemini 2.5 Flash. Text/structured modules default On when the model is configured.</p>

          <div class="mt-4">
            <h3 class="text-sm font-semibold text-slate-800 dark:text-slate-200 mb-2">Toggle AI Modules</h3>
            <form action="<%= update_ai_caps_ops_admin_index_path %>" method="post" class="space-y-3" data-controller="submit-form">
              <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
              <label class="inline-flex items-center gap-3 text-sm font-medium text-slate-700 dark:text-slate-300">
                <input type="hidden" name="ai_images" value="0">
                <input type="checkbox" name="ai_images" value="1" <%= 'checked' if @ai_caps[:image_generation] %> class="<%= ui_checkbox_class %>">
                <span>Enable AI Image Generation</span>
              </label>
              <label class="inline-flex items-center gap-3 text-sm font-medium text-slate-700 dark:text-slate-300">
                <input type="hidden" name="ai_image_descriptions" value="0">
                <input type="checkbox" name="ai_image_descriptions" value="1" <%= 'checked' if @ai_caps[:image_descriptions] %> class="<%= ui_checkbox_class %>">
                <span>Enable AI Image Descriptions</span>
              </label>
              <label class="inline-flex items-center gap-3 text-sm font-medium text-slate-700 dark:text-slate-300">
                <input type="hidden" name="ai_text" value="0">
                <input type="checkbox" name="ai_text" value="1" <%= 'checked' if FeatureFlags.enabled?(:ai_text) %> class="<%= ui_checkbox_class %>">
                <span>Enable AI Text (Traits)</span>
              </label>
              <label class="inline-flex items-center gap-3 text-sm font-medium text-slate-700 dark:text-slate-300">
                <input type="hidden" name="ai_structured_output" value="0">
                <input type="checkbox" name="ai_structured_output" value="1" <%= 'checked' if FeatureFlags.enabled?(:ai_structured_output) %> class="<%= ui_checkbox_class %>">
                <span>Enable AI Structured Output</span>
              </label>
              <div class="pt-2">
                <button type="submit" class="<%= ui_primary_button_class %>" data-submit-form-target="button">
                  <span data-submit-form-target="spinner" class="hidden"><%= ui_spinner_icon %></span>
                  <span data-submit-form-target="text">Save AI settings</span>
                </button>
              </div>
            </form>
          </div>
        </div>

        <!-- Axiom Observability -->
        <div class="rounded border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-800">
          <h2 class="mb-3 text-lg font-semibold text-slate-900 dark:text-slate-100">Axiom Observability</h2>
          
          <div class="mb-3">
            <h3 class="text-sm font-semibold text-slate-700 dark:text-slate-300 mb-2">Streaming Datasets</h3>
            <div class="flex flex-wrap gap-2">
              <% if @axiom[:dataset_url].present? %>
                <a href="<%= @axiom[:dataset_url] %>" target="_blank" rel="noopener" class="rounded border border-blue-200 bg-blue-50 px-3 py-1.5 text-sm text-blue-700 hover:bg-blue-100 dark:border-blue-800 dark:bg-blue-900/20 dark:text-blue-300 dark:hover:bg-blue-900/30">
                  Logs Dataset
                </a>
              <% end %>
              <% traces_dataset_url = @axiom[:traces_dataset_url].presence || @axiom[:metrics_dataset_url] %>
              <% if traces_dataset_url.present? %>
                <a href="<%= traces_dataset_url %>" target="_blank" rel="noopener" class="rounded border border-emerald-200 bg-emerald-50 px-3 py-1.5 text-sm text-emerald-700 hover:bg-emerald-100 dark:border-emerald-800 dark:bg-emerald-900/20 dark:text-emerald-300 dark:hover:bg-emerald-900/30">
                  Traces Dataset
                </a>
              <% end %>
            </div>
          </div>

          <div class="mb-3">
            <h3 class="text-sm font-semibold text-slate-700 dark:text-slate-300 mb-2">OpenTelemetry Traces</h3>
            <div class="flex flex-wrap gap-2">
              <% if @axiom[:traces_url].present? %>
                <a href="<%= @axiom[:traces_url] %>" target="_blank" rel="noopener" class="rounded border border-purple-200 bg-purple-50 px-3 py-1.5 text-sm text-purple-700 hover:bg-purple-100 dark:border-purple-800 dark:bg-purple-900/20 dark:text-purple-300 dark:hover:bg-purple-900/30">
                  OTEL Traces
                </a>
              <% end %>
            </div>
            <p class="mt-2 text-xs text-slate-500 dark:text-slate-400">OTEL traces are automatically collected and viewable in Axiom's traces view when configured.</p>
          </div>

          <div class="mb-3">
            <h3 class="text-sm font-semibold text-slate-700 dark:text-slate-300 mb-2">Axiom / OTEL Status</h3>
            <div class="flex flex-wrap gap-2 text-xs">
              <% if defined?(@axiom_status) && @axiom_status.present? %>
                <% allowed = @axiom_status[:forwarding_allowed] %>
                <span class="rounded bg-<%= allowed ? 'emerald' : 'red' %>-100 px-2 py-0.5 text-<%= allowed ? 'emerald' : 'red' %>-800 dark:bg-<%= allowed ? 'emerald' : 'red' %>-900/30 dark:text-<%= allowed ? 'emerald' : 'red' %>-300">Forwarding <%= allowed ? 'Allowed' : 'Blocked' %></span>
                <span class="rounded bg-slate-100 px-2 py-0.5 text-slate-800 dark:bg-slate-900/30 dark:text-slate-300">Reason <%= @axiom_status[:forwarding_reason] %></span>
                <span class="rounded bg-<%= @axiom_status[:token_present] ? 'emerald' : 'red' %>-100 px-2 py-0.5 text-<%= @axiom_status[:token_present] ? 'emerald' : 'red' %>-800 dark:bg-<%= @axiom_status[:token_present] ? 'emerald' : 'red' %>-900/30 dark:text-<%= @axiom_status[:token_present] ? 'emerald' : 'red' %>-300">Token <%= @axiom_status[:token_present] ? 'Present' : 'Missing' %></span>
                <span class="rounded bg-<%= @axiom_status[:dataset_present] ? 'emerald' : 'red' %>-100 px-2 py-0.5 text-<%= @axiom_status[:dataset_present] ? 'emerald' : 'red' %>-800 dark:bg-<%= @axiom_status[:dataset_present] ? 'emerald' : 'red' %>-900/30 dark:text-<%= @axiom_status[:dataset_present] ? 'emerald' : 'red' %>-300">Logs dataset <%= @axiom_status[:dataset_present] ? 'Set' : 'Unset' %></span>
                <span class="rounded bg-<%= @axiom_status[:metrics_dataset_present] ? 'emerald' : 'slate' %>-100 px-2 py-0.5 text-<%= @axiom_status[:metrics_dataset_present] ? 'emerald' : 'slate' %>-800 dark:bg-<%= @axiom_status[:metrics_dataset_present] ? 'emerald' : 'slate' %>-900/30 dark:text-<%= @axiom_status[:metrics_dataset_present] ? 'emerald' : 'slate' %>-300">Traces dataset <%= @axiom_status[:metrics_dataset_present] ? 'Set' : 'Unset' %></span>
                <% if @axiom_status[:otel_endpoint].present? %>
                  <span class="rounded bg-emerald-100 px-2 py-0.5 text-emerald-800 dark:bg-emerald-900/30 dark:text-emerald-300">OTEL endpoint set</span>
                <% else %>
                  <span class="rounded bg-slate-100 px-2 py-0.5 text-slate-800 dark:bg-slate-900/30 dark:text-slate-300">OTEL endpoint default/none</span>
                <% end %>
                <span class="rounded bg-slate-100 px-2 py-0.5 text-slate-800 dark:bg-slate-900/30 dark:text-slate-300">Env <%= @axiom_status[:env] %></span>
              <% else %>
                <span class="rounded bg-slate-100 px-2 py-0.5 text-slate-800 dark:bg-slate-900/30 dark:text-slate-300">Status unavailable</span>
              <% end %>
            </div>
            <% if defined?(@axiom_status) && @axiom_status.present? %>
              <% queue = @axiom_status[:queue] || {} %>
              <dl class="mt-2 grid gap-1 text-xs text-slate-500 dark:text-slate-400">
                <div>Queue: enqueued=<%= queue[:enqueued] || 0 %>, delivered=<%= queue[:delivered] || 0 %>, pending=<%= queue[:pending] || 0 %></div>
                <div>Last delivery: <%= queue[:last_delivery]&.dig(:at) || '—' %></div>
                <div>Last error: <%= queue[:last_error] || '—' %></div>
                <div>Last skip: <%= queue[:last_skip] ? "#{queue[:last_skip][:reason]} @ #{queue[:last_skip][:at]}" : '—' %></div>
              </dl>
            <% end %>
            <p class="mt-2 text-xs text-slate-500 dark:text-slate-400">Forwarding runs in production when credentials are present, or in other environments when <code>AXIOM_ENABLED=1</code>. Run <code>rake axiom:runtime_doctor</code> to exercise the StructuredLogger queue end-to-end.</p>
          </div>

          <div>
            <h3 class="text-sm font-semibold text-slate-700 dark:text-slate-300 mb-2">Test & Debug</h3>
            <div class="grid gap-3 md:grid-cols-2">
              <div class="rounded border border-slate-200 p-3 dark:border-slate-700">
                <h4 class="text-sm font-semibold mb-2">StructuredLogger</h4>
                <form action="<%= ops_axiom_log_test_path %>" method="post" class="space-y-3" data-controller="submit-form">
                  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
                  <div class="grid grid-cols-1 gap-2 sm:grid-cols-2">
                    <div class="space-y-1.5">
                      <label class="text-xs font-semibold uppercase tracking-wide text-slate-500 dark:text-slate-400">Level</label>
                      <select name="level" class="<%= "#{ui_select_class} h-auto" %>">
                        <option>info</option>
                        <option>warn</option>
                        <option>error</option>
                        <option>debug</option>
                      </select>
                    </div>
                    <div class="space-y-1.5">
                      <label class="text-xs font-semibold uppercase tracking-wide text-slate-500 dark:text-slate-400">Force send</label>
                      <select name="force" class="<%= "#{ui_select_class} h-auto" %>">
                        <option value="0">No</option>
                        <option value="1">Yes</option>
                      </select>
                    </div>
                  </div>
                  <div class="space-y-1.5">
                    <label class="text-xs font-semibold uppercase tracking-wide text-slate-500 dark:text-slate-400">Message</label>
                    <input type="text" name="message" placeholder="custom message" class="<%= ui_input_class %>">
                  </div>
                  <div class="space-y-1.5">
                    <label class="text-xs font-semibold uppercase tracking-wide text-slate-500 dark:text-slate-400">Payload (JSON)</label>
                    <textarea name="payload" rows="3" class="<%= "#{ui_textarea_class} font-mono" %>" placeholder='{"kind":"test","code":123}'></textarea>
                  </div>
                  <button type="submit" class="<%= ui_primary_button_class %>" data-submit-form-target="button">
                    <span data-submit-form-target="spinner" class="hidden"><%= ui_spinner_icon %></span>
                    <span data-submit-form-target="text">Send structured log</span>
                  </button>
                </form>
              </div>

              <div class="rounded border border-slate-200 p-3 dark:border-slate-700">
                <h4 class="text-sm font-semibold mb-2">Direct Ingest</h4>
                <form action="<%= ops_axiom_direct_ingest_path %>" method="post" class="space-y-3" data-controller="submit-form">
                  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
                  <div class="space-y-1.5">
                    <label class="text-xs font-semibold uppercase tracking-wide text-slate-500 dark:text-slate-400">Dataset</label>
                    <input type="text" name="dataset" placeholder="<%= AppConfig.axiom[:dataset] %>" class="<%= ui_input_class %>">
                  </div>
                  <div class="space-y-1.5">
                    <label class="text-xs font-semibold uppercase tracking-wide text-slate-500 dark:text-slate-400">Body (JSON array or object)</label>
                    <textarea name="body" rows="4" class="<%= "#{ui_textarea_class} font-mono" %>" placeholder='[{"ts":"<%= Time.now.utc.iso8601 %>","level":"INFO","message":"ops_direct","env":"<%= Rails.env %>"}]'></textarea>
                  </div>
                  <button type="submit" class="<%= ui_primary_button_class %>" data-submit-form-target="button">
                    <span data-submit-form-target="spinner" class="hidden"><%= ui_spinner_icon %></span>
                    <span data-submit-form-target="text">Ingest payload</span>
                  </button>
                </form>
              </div>

              <div class="rounded border border-slate-200 p-3 dark:border-slate-700 md:col-span-2">
                <h4 class="text-sm font-semibold mb-2">OpenTelemetry Span</h4>
                <form action="<%= ops_axiom_otel_smoke_path %>" method="post" class="space-y-3" data-controller="submit-form">
                  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
                  <div class="grid grid-cols-1 gap-2 sm:grid-cols-3">
                    <div class="space-y-1.5">
                      <label class="text-xs font-semibold uppercase tracking-wide text-slate-500 dark:text-slate-400">Name</label>
                      <input type="text" name="name" placeholder="ops_otel_smoke" class="<%= ui_input_class %>">
                    </div>
                    <div class="space-y-1.5">
                      <label class="text-xs font-semibold uppercase tracking-wide text-slate-500 dark:text-slate-400">Mark error</label>
                      <select name="error" class="<%= "#{ui_select_class} h-auto" %>">
                        <option value="0">No</option>
                        <option value="1">Yes</option>
                      </select>
                    </div>
                  </div>
                  <div class="space-y-1.5">
                    <label class="text-xs font-semibold uppercase tracking-wide text-slate-500 dark:text-slate-400">Attributes (JSON object)</label>
                    <textarea name="attributes" rows="3" class="<%= "#{ui_textarea_class} font-mono" %>" placeholder='{"smoke":true,"env":"<%= Rails.env %>"}'></textarea>
                  </div>
                  <button type="submit" class="<%= ui_primary_button_class %>" data-submit-form-target="button">
                    <span data-submit-form-target="spinner" class="hidden"><%= ui_spinner_icon %></span>
                    <span data-submit-form-target="text">Emit OTEL span</span>
                  </button>
                </form>
              </div>
            </div>
            <p class="mt-2 text-xs text-slate-500 dark:text-slate-400">Tip: Use StructuredLogger for logs into the logs dataset, Direct Ingest to bypass forwarding gates or to target metrics dataset, and OTEL Span to verify traces/attributes and error status.</p>
          </div>
        </div>
      </div>

      <!-- Pipeline Tab -->
      <div data-tabs-target="panel" data-tab-panel="pipeline" class="hidden space-y-6">
        <div class="rounded border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-800">
          <h2 class="mb-3 text-lg font-semibold text-slate-900 dark:text-slate-100">Pipeline Stages</h2>
          <p class="text-sm text-slate-600 dark:text-slate-300 mb-3">Visibility of stages, gates, and outputs. Edit code in <code>Profiles::GeneratePipelineService</code>; this list helps you see everything in one place.</p>
          <div class="grid gap-2 sm:grid-cols-2">
            <% @pipeline_manifest.each do |st| %>
              <div class="rounded border border-slate-200 p-3 dark:border-slate-700">
                <div class="flex items-center justify-between">
                  <div class="text-slate-800 dark:text-slate-200 text-sm font-medium"><%= st[:label] %></div>
                  <% if st[:enabled] %>
                    <span class="rounded bg-emerald-100 px-2 py-0.5 text-xs font-semibold text-emerald-800 dark:bg-emerald-900/30 dark:text-emerald-300">On</span>
                  <% else %>
                    <span class="rounded bg-slate-100 px-2 py-0.5 text-xs font-semibold text-slate-700 dark:bg-slate-800 dark:text-slate-300">Off</span>
                  <% end %>
                </div>
                <div class="mt-1 text-xs text-slate-600 dark:text-slate-400"><%= st[:description] %></div>
                <div class="mt-2 text-[11px] text-slate-500 dark:text-slate-400">Produces: <%= Array(st[:produces]).join(', ') %></div>
                <% if st[:gated_by].present? %>
                  <div class="mt-1 text-[11px] text-slate-500 dark:text-slate-400">Gated by: <code><%= st[:gated_by] %></code></div>
                <% end %>
              </div>
            <% end %>
          </div>
        </div>

        <div class="rounded border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-800">
          <div class="flex flex-col gap-3 md:flex-row md:items-center md:justify-between">
            <div>
              <h2 class="text-lg font-semibold text-slate-900 dark:text-slate-100">Pipeline Snapshot</h2>
              <p class="text-sm text-slate-600 dark:text-slate-300">Inspect saved artifacts from <code>tmp/pipeline_runs</code> for a GitHub login. Snapshots are written via <code>rake pipeline:run</code>.</p>
            </div>
            <form action="<%= ops_admin_path %>" method="get" class="flex items-center gap-2">
              <input type="hidden" name="tab" value="pipeline">
              <label class="text-sm font-medium text-slate-600 dark:text-slate-300" for="pipeline_login_select">Login</label>
              <select id="pipeline_login_select" name="pipeline_login" class="<%= "#{ui_select_class} h-auto w-48" %>">
                <% @pipeline_snapshot_logins.each do |login| %>
                  <option value="<%= login %>" <%= "selected" if login == @pipeline_snapshot_login %>>@<%= login %></option>
                <% end %>
              </select>
              <button type="submit" class="<%= ui_secondary_button_class %>">Load</button>
            </form>
          </div>

          <% if @pipeline_snapshot.present? %>
            <% snapshot = @pipeline_snapshot[:snapshot] || {} %>
            <% run_meta = @pipeline_snapshot[:metadata] || {} %>
            <% stage_metadata = @pipeline_snapshot[:stage_metadata] || {} %>
            <% captures = snapshot[:captures] || {} %>
            <div class="mt-4 space-y-4 text-sm text-slate-700 dark:text-slate-200">
              <div class="grid gap-3 md:grid-cols-2">
                <div class="rounded border border-slate-200 p-3 dark:border-slate-700">
                  <h3 class="text-sm font-semibold text-slate-800 dark:text-slate-100">Run Details</h3>
                  <dl class="mt-2 space-y-1">
                    <div class="flex justify-between gap-2">
                      <dt class="text-slate-500 dark:text-slate-400">Run ID</dt>
                      <dd class="font-mono text-xs"><%= run_meta[:run_id] %></dd>
                    </div>
                    <div class="flex justify-between gap-2">
                      <dt class="text-slate-500 dark:text-slate-400">Duration</dt>
                      <dd><%= run_meta[:duration_ms] ? "#{run_meta[:duration_ms]} ms" : "n/a" %></dd>
                    </div>
                    <div class="flex justify-between gap-2">
                      <dt class="text-slate-500 dark:text-slate-400">Saved Path</dt>
                      <dd class="font-mono text-xs break-all"><%= @pipeline_snapshot[:path] %></dd>
                    </div>
                  </dl>
                </div>
                <div class="rounded border border-slate-200 p-3 dark:border-slate-700">
                  <h3 class="text-sm font-semibold text-slate-800 dark:text-slate-100">Key Files</h3>
                  <% files = Array(@pipeline_snapshot[:files]) %>
                  <% if files.any? %>
                    <ul class="mt-2 space-y-1">
                      <% files.select { |f| f.ends_with?(".json") }.each do |fname| %>
                        <li>
                          <a class="text-indigo-600 hover:underline dark:text-indigo-400" href="<%= ops_pipeline_snapshot_path(login: @pipeline_snapshot_login, file: fname) %>"><%= fname %></a>
                        </li>
                      <% end %>
                    </ul>
                  <% else %>
                    <p class="text-slate-500 dark:text-slate-400">No files detected.</p>
                  <% end %>
                </div>
              </div>

              <div>
                <h3 class="text-sm font-semibold text-slate-800 dark:text-slate-100 mb-2">Stage Timings</h3>
                <div class="overflow-x-auto rounded border border-slate-200 dark:border-slate-700">
                  <table class="min-w-full divide-y divide-slate-200 text-xs dark:divide-slate-700">
                    <thead class="bg-slate-100 dark:bg-slate-800">
                      <tr>
                        <th class="px-3 py-2 text-left font-semibold text-slate-600 dark:text-slate-300">Stage</th>
                        <th class="px-3 py-2 text-left font-semibold text-slate-600 dark:text-slate-300">Status</th>
                        <th class="px-3 py-2 text-left font-semibold text-slate-600 dark:text-slate-300">Duration</th>
                        <th class="px-3 py-2 text-left font-semibold text-slate-600 dark:text-slate-300">Notes</th>
                      </tr>
                    </thead>
                    <tbody class="divide-y divide-slate-100 dark:divide-slate-800">
                      <% Profiles::GeneratePipelineService::STAGES.each do |stage| %>
                        <% meta = stage_metadata[stage.id] || stage_metadata[stage.id.to_s] %>
                        <% next unless meta.present? %>
                        <% details = meta[:metadata].is_a?(Hash) ? meta[:metadata] : {} %>
                        <% notes = [] %>
                        <% notes << details[:reason] if details[:reason].present? %>
                        <% notes << details[:message] if details[:message].present? %>
                        <% notes << details[:upstream_error] if details[:upstream_error].present? %>
                        <% notes << "heuristic" if details[:heuristic] %>
                        <% notes << "mock" if details[:mock] %>
                        <% notes << meta[:error] if meta[:error].present? %>
                        <tr>
                          <td class="px-3 py-2 font-medium text-slate-700 dark:text-slate-100"><%= stage.label %></td>
                          <td class="px-3 py-2 text-slate-600 dark:text-slate-300"><%= meta[:status] || (meta[:degraded] ? "degraded" : "ok") %></td>
                          <td class="px-3 py-2 text-slate-600 dark:text-slate-300"><%= meta[:duration_ms] ? "#{meta[:duration_ms]} ms" : "n/a" %></td>
                          <td class="px-3 py-2 text-slate-600 dark:text-slate-300"><%= notes.compact.uniq.join(" / ").presence || "—" %></td>
                        </tr>
                      <% end %>
                    </tbody>
                  </table>
                </div>
              </div>

              <div class="grid gap-3 md:grid-cols-2">
                <% github = snapshot[:github_payload] || {} %>
                <div class="rounded border border-slate-200 p-3 dark:border-slate-700">
                  <h3 class="text-sm font-semibold text-slate-800 dark:text-slate-100">GitHub Payload</h3>
                  <% profile = github[:profile] || {} %>
                  <% if profile.present? %>
                    <ul class="mt-2 space-y-1">
                      <li><span class="font-semibold">Login:</span> @<%= profile[:login] %></li>
                      <li><span class="font-semibold">Name:</span> <%= profile[:name] %></li>
                      <li><span class="font-semibold">Followers:</span> <%= profile[:followers] %></li>
                      <li><span class="font-semibold">Public Repos:</span> <%= profile[:public_repos] %></li>
                    </ul>
                    <% if github[:summary].present? %>
                      <p class="mt-2 text-xs text-slate-500 dark:text-slate-400"><%= github[:summary] %></p>
                    <% end %>
                  <% else %>
                    <p class="text-slate-500 dark:text-slate-400">No GitHub payload stored.</p>
                  <% end %>
                </div>

                <% card = snapshot[:card] || {} %>
                <div class="rounded border border-slate-200 p-3 dark:border-slate-700">
                  <h3 class="text-sm font-semibold text-slate-800 dark:text-slate-100">AI Card</h3>
                  <% if card.present? %>
                    <ul class="mt-2 space-y-1">
                      <li><span class="font-semibold">Title:</span> <%= card[:title] %></li>
                      <li><span class="font-semibold">Tagline:</span> <%= card[:tagline] %></li>
                      <li><span class="font-semibold">Vibe:</span> <%= card[:vibe] %></li>
                      <li><span class="font-semibold">Model:</span> <%= card[:ai_model] %></li>
                    </ul>
                  <% else %>
                    <p class="text-slate-500 dark:text-slate-400">No AI card captured.</p>
                  <% end %>
                </div>
              </div>

              <% ai_meta = @pipeline_snapshot[:ai_metadata] || {} %>
              <% if ai_meta.present? %>
                <div class="rounded border border-slate-200 p-3 dark:border-slate-700">
                  <h3 class="text-sm font-semibold text-slate-800 dark:text-slate-100">AI Prompt & Response</h3>
                  <% prompt = @pipeline_snapshot[:ai_prompt] || {} %>
                  <% if prompt[:system_prompt].present? %>
                    <details class="mt-2">
                      <summary class="cursor-pointer text-indigo-600 hover:underline dark:text-indigo-400">System Prompt</summary>
                      <pre class="mt-2 max-h-64 overflow-auto rounded bg-slate-900 p-3 text-xs text-slate-100"><%= prompt[:system_prompt] %></pre>
                    </details>
                  <% end %>
                  <% if prompt[:context].present? %>
                    <details class="mt-2">
                      <summary class="cursor-pointer text-indigo-600 hover:underline dark:text-indigo-400">Prompt Context JSON</summary>
                      <pre class="mt-2 max-h-64 overflow-auto rounded bg-slate-900 p-3 text-xs text-slate-100"><%= JSON.pretty_generate(prompt[:context]) %></pre>
                    </details>
                  <% end %>
                  <% response_preview = ai_meta.dig(:metadata, :response_preview) %>
                  <% if response_preview.present? %>
                    <p class="mt-2 text-xs text-slate-500 dark:text-slate-400">Response preview: <%= response_preview %></p>
                  <% end %>
                </div>
              <% end %>

              <div class="rounded border border-slate-200 p-3 dark:border-slate-700">
                <h3 class="text-sm font-semibold text-slate-800 dark:text-slate-100 mb-2">Screenshot Variants</h3>
                <% if captures.present? %>
                  <div class="overflow-x-auto">
                    <table class="min-w-full divide-y divide-slate-200 text-xs dark:divide-slate-700">
                      <thead class="bg-slate-100 dark:bg-slate-800">
                        <tr>
                          <th class="px-3 py-2 text-left font-semibold text-slate-600 dark:text-slate-300">Variant</th>
                          <th class="px-3 py-2 text-left font-semibold text-slate-600 dark:text-slate-300">Local</th>
                          <th class="px-3 py-2 text-left font-semibold text-slate-600 dark:text-slate-300">Public URL</th>
                        </tr>
                      </thead>
                      <tbody class="divide-y divide-slate-100 dark:divide-slate-800">
                        <% captures.each do |variant, data| %>
                          <tr>
                            <td class="px-3 py-2 font-medium text-slate-700 dark:text-slate-100"><%= variant %></td>
                            <td class="px-3 py-2 font-mono text-xs break-all text-slate-500 dark:text-slate-400"><%= data[:local_path] %></td>
                            <td class="px-3 py-2">
                              <% if data[:public_url].present? %>
                                <a class="text-indigo-600 hover:underline dark:text-indigo-400" href="<%= data[:public_url] %>" target="_blank" rel="noopener">Open</a>
                              <% else %>
                                <span class="text-slate-500 dark:text-slate-400">n/a</span>
                              <% end %>
                            </td>
                          </tr>
                        <% end %>
                      </tbody>
                    </table>
                  </div>
                <% else %>
                  <p class="text-slate-500 dark:text-slate-400">No screenshot captures recorded.</p>
                <% end %>
              </div>
            </div>
          <% elsif @pipeline_snapshot_logins.any? %>
            <p class="mt-3 text-sm text-slate-500 dark:text-slate-400">Select a login with a saved snapshot to view details.</p>
          <% else %>
            <p class="mt-3 text-sm text-slate-500 dark:text-slate-400">No pipeline snapshots found. Run <code>rake pipeline:run[login]</code> to create one.</p>
          <% end %>
        </div>

        <!-- Pipeline Doctor -->
        <div class="rounded border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-800">
          <h2 class="mb-3 text-lg font-semibold text-slate-900 dark:text-slate-100">Pipeline Doctor</h2>
          <p class="text-sm text-slate-600 dark:text-slate-300 mb-3">Run an end-to-end readiness check for a GitHub login. This fetches GitHub data, syncs a profile, generates traits, and captures screenshots for selected variants.</p>
          <form action="<%= ops_pipeline_doctor_path %>" method="post" class="grid gap-3 sm:grid-cols-2" data-controller="submit-form">
            <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
            <div class="space-y-1.5">
              <label class="text-sm font-semibold text-slate-700 dark:text-slate-200">GitHub login</label>
              <input type="text" name="login" placeholder="loftwah" class="<%= ui_input_class %>" required />
            </div>
            <div class="space-y-1.5">
              <label class="text-sm font-semibold text-slate-700 dark:text-slate-200">Host (optional)</label>
              <input type="text" name="host" placeholder="http://127.0.0.1:3000" class="<%= ui_input_class %>" />
            </div>
            <div class="space-y-1.5">
              <label class="text-sm font-semibold text-slate-700 dark:text-slate-200">Email results to (optional)</label>
              <input type="email" name="email" placeholder="ops@example.com" class="<%= ui_input_class %>" />
            </div>
            <div class="space-y-1.5">
              <label class="text-sm font-semibold text-slate-700 dark:text-slate-200">Variants (comma/space separated)</label>
              <input type="text" name="variants" placeholder="og, card, simple, banner" class="<%= ui_input_class %>" />
            </div>
            <div class="sm:col-span-2">
              <button type="submit" class="<%= ui_primary_button_class %>" data-submit-form-target="button">
                <span data-submit-form-target="spinner" class="hidden"><%= ui_spinner_icon %></span>
                <span data-submit-form-target="text">Run pipeline doctor</span>
              </button>
            </div>
          </form>
          <% if flash[:doctor_json].present? %>
            <div class="mt-3 rounded border border-slate-200 bg-slate-50 p-3 text-xs text-slate-800 dark:border-slate-700 dark:bg-slate-900 dark:text-slate-200">
              <pre class="whitespace-pre-wrap"><%= JSON.pretty_generate(JSON.parse(flash[:doctor_json])) rescue flash[:doctor_json] %></pre>
            </div>
          <% end %>
        </div>

        <!-- Screenshots Health -->
        <div class="rounded border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-800">
          <h2 class="mb-2 text-lg font-semibold text-slate-900 dark:text-slate-100">Screenshots Health</h2>
          <ul class="text-sm text-slate-700 dark:text-slate-300 list-disc pl-5">
            <li>Node: <code>node</code> must be available</li>
            <li>Puppeteer: <code>npm i --save-dev puppeteer</code> installed</li>
            <li>Script: <code>script/screenshot.js</code> executable</li>
          </ul>
          <p class="mt-2 text-xs text-slate-500 dark:text-slate-400">Failed screenshot jobs will include stderr in logs as <code>screenshot_command_failed</code> or <code>screenshot_empty</code>.</p>
        </div>
      </div>

      <!-- Access Tab -->
      <div data-tabs-target="panel" data-tab-panel="access" class="hidden space-y-6">
        <div class="rounded border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-800">
          <h2 class="mb-3 text-lg font-semibold text-slate-900 dark:text-slate-100">Access Control</h2>
          <p class="text-sm text-slate-700 dark:text-slate-300 mb-3">Gate sign-in to approved GitHub accounts until launch. Toggle open access to let anyone sign in. Settings persist in the database.</p>
          <form action="<%= update_access_ops_admin_index_path %>" method="post" class="space-y-4" data-controller="submit-form">
            <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
            <label class="inline-flex items-center gap-3 text-sm font-medium text-slate-800 dark:text-slate-200">
              <input type="hidden" name="open_access" value="0">
              <input type="checkbox" id="open_access" name="open_access" value="1" <%= 'checked' if @open_access %> class="<%= ui_checkbox_class %>">
              <span>Open access (allow all accounts)</span>
            </label>
            <div class="space-y-1.5">
              <label class="text-sm font-medium text-slate-700 dark:text-slate-300" for="allowed_logins">Allowed GitHub logins (comma or space separated)</label>
              <textarea id="allowed_logins" name="allowed_logins" rows="3" class="<%= ui_textarea_class %>" placeholder="loftwah, jrh89"><%= h @allowed_logins %></textarea>
            </div>
            <button type="submit" class="<%= ui_primary_button_class %>" data-submit-form-target="button">
              <span data-submit-form-target="spinner" class="hidden"><%= ui_spinner_icon %></span>
              <span data-submit-form-target="text">Save access settings</span>
            </button>
          </form>
        </div>

        <div class="rounded border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-800">
          <h2 class="mb-3 text-lg font-semibold text-slate-900 dark:text-slate-100">Invite Settings</h2>
          <p class="text-sm text-slate-700 dark:text-slate-300 mb-3">Control invite usage cap and optionally override the invite codes list (stored in credentials by default). Overrides persist in <code>AppSetting</code>.</p>
          <form action="<%= update_invites_ops_admin_index_path %>" method="post" class="space-y-4" data-controller="submit-form">
            <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
            <div class="grid gap-3 sm:grid-cols-2">
              <div class="space-y-1.5">
                <label class="text-sm font-medium text-slate-700 dark:text-slate-300">Invite cap limit</label>
                <input type="number" min="0" step="1" name="invite_cap_limit" value="<%= @invite_cap_limit %>" class="<%= ui_input_class %>">
                <p class="text-xs text-slate-500 dark:text-slate-400">Total successful invite sign-ins allowed before invites close.</p>
              </div>
              <div class="space-y-1.5">
                <label class="text-sm font-medium text-slate-700 dark:text-slate-300">Invite cap used</label>
                <input type="number" min="0" step="1" name="invite_cap_used" value="<%= @invite_cap_used %>" class="<%= ui_input_class %>">
                <p class="text-xs text-slate-500 dark:text-slate-400">Adjust for manual corrections; incremented automatically when an invite grants access.</p>
              </div>
            </div>
            <div class="space-y-1.5">
              <label class="text-sm font-medium text-slate-700 dark:text-slate-300">Invite codes override (comma/space separated)</label>
              <textarea name="invite_codes_override" rows="3" class="<%= ui_textarea_class %>" placeholder="code1, code2, code3"><%= h @invite_codes_override %></textarea>
              <p class="text-xs text-slate-500 dark:text-slate-400">Leave blank to clear the override and fall back to encrypted credentials.</p>
            </div>
            <button type="submit" class="<%= ui_primary_button_class %>" data-submit-form-target="button">
              <span data-submit-form-target="spinner" class="hidden"><%= ui_spinner_icon %></span>
              <span data-submit-form-target="text">Save invite settings</span>
            </button>
            <div class="text-xs text-slate-500 dark:text-slate-400">Current: used <%= @invite_cap_used %> of <%= @invite_cap_limit %>. <%= Access::InviteCodes.exhausted? ? 'Exhausted' : 'Open' %>.</div>
          </form>
        </div>
      </div>

      <!-- System Tab -->
      <div data-tabs-target="panel" data-tab-panel="system" class="hidden space-y-6">
        <!-- Database Backups & System Stats Grid -->
        <div class="grid gap-4 sm:grid-cols-2">
          <!-- Database Backups -->
          <div class="rounded border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-800">
            <div class="text-sm text-slate-500 dark:text-slate-400">Database Backups</div>
            <div class="mt-1 text-slate-700 dark:text-slate-300 text-sm">Upload SQLite files in <code>storage/</code> to object storage and prune old backups. Configure <code>BACKUP_BUCKET</code> (or <code>DO_SPACES_BUCKET</code>) and optional <code>BACKUP_PREFIX</code>.</div>
            <div class="mt-3 flex flex-wrap gap-2">
              <form action="<%= backups_create_ops_admin_index_path %>" method="post" data-controller="submit-form" onsubmit="return confirm('Create a new backup now?');">
                <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
                <button type="submit" class="<%= ui_primary_button_class %> px-4" data-submit-form-target="button">
                  <span data-submit-form-target="spinner" class="hidden"><%= ui_spinner_icon(classes: "h-3.5 w-3.5") %></span>
                  <span data-submit-form-target="text">Create backup</span>
                </button>
              </form>
              <form action="<%= backups_prune_ops_admin_index_path %>" method="post" data-controller="submit-form" onsubmit="return confirm('Prune old backups now?');">
                <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
                <button type="submit" class="<%= ui_secondary_button_class %> px-4" data-submit-form-target="button">
                  <span data-submit-form-target="spinner" class="hidden"><%= ui_spinner_icon(classes: "h-3.5 w-3.5") %></span>
                  <span data-submit-form-target="text">Prune old</span>
                </button>
              </form>
              <form action="<%= backups_doctor_ops_admin_index_path %>" method="post" data-controller="submit-form">
                <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
                <button type="submit" class="<%= ui_secondary_button_class %> px-4" data-submit-form-target="button">
                  <span data-submit-form-target="spinner" class="hidden"><%= ui_spinner_icon(classes: "h-3.5 w-3.5") %></span>
                  <span data-submit-form-target="text">Check status</span>
                </button>
              </form>
              <form action="<%= backups_doctor_write_ops_admin_index_path %>" method="post" data-controller="submit-form" onsubmit="return confirm('Perform write+delete probe?');">
                <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
                <input type="hidden" name="confirm" value="YES" />
                <button type="submit" class="<%= ui_secondary_button_class %> px-4" data-submit-form-target="button">
                  <span data-submit-form-target="spinner" class="hidden"><%= ui_spinner_icon(classes: "h-3.5 w-3.5") %></span>
                  <span data-submit-form-target="text">Write probe</span>
                </button>
              </form>
            </div>
            <div class="mt-2 text-xs text-slate-500 dark:text-slate-400">Retention via env: <code>BACKUP_RETENTION_DAYS</code> (default 14), <code>BACKUP_KEEP_MIN</code> (default 7). Scheduled daily via recurring.yml.</div>
          </div>

          <!-- Job Adapter -->
          <div class="rounded border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-800">
            <div class="text-sm text-slate-500 dark:text-slate-400">Adapter</div>
            <div class="text-lg font-semibold text-slate-900 dark:text-slate-100"><%= @adapter.class.name %></div>
          </div>

          <!-- Job Stats -->
          <div class="rounded border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-800">
            <div class="text-sm text-slate-500 dark:text-slate-400">Queued</div>
            <div class="text-lg font-semibold text-slate-900 dark:text-slate-100"><%= @stats[:queued] || '—' %></div>
          </div>
          <div class="rounded border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-800">
            <div class="text-sm text-slate-500 dark:text-slate-400">Ready</div>
            <div class="text-lg font-semibold text-slate-900 dark:text-slate-100"><%= @stats[:ready] || '—' %></div>
          </div>
          <div class="rounded border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-800">
            <div class="text-sm text-slate-500 dark:text-slate-400">Running</div>
            <div class="text-lg font-semibold text-slate-900 dark:text-slate-100"><%= @stats[:running] || '—' %></div>
          </div>
          <div class="rounded border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-800">
            <div class="text-sm text-slate-500 dark:text-slate-400">Failed</div>
            <div class="text-lg font-semibold text-slate-900 dark:text-slate-100"><%= @stats[:failed] || '—' %></div>
          </div>
          <div class="rounded border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-800">
            <div class="text-sm text-slate-500 dark:text-slate-400">Finished (1h)</div>
            <div class="text-lg font-semibold text-slate-900 dark:text-slate-100"><%= @stats[:finished_last_hour] || '—' %></div>
          </div>
        </div>

        <!-- Leaderboards -->
        <div class="rounded border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-800">
          <h2 class="text-lg font-semibold mb-2">Leaderboards</h2>
          <div class="flex flex-wrap gap-2">
            <form action="<%= rebuild_leaderboards_ops_admin_index_path %>" method="post" data-controller="submit-form">
              <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
              <button type="submit" class="<%= ui_primary_button_class %> px-4" data-submit-form-target="button">
                <span data-submit-form-target="spinner" class="hidden"><%= ui_spinner_icon(classes: "h-3.5 w-3.5") %></span>
                <span data-submit-form-target="text">Rebuild all</span>
              </button>
            </form>
            <form action="<%= capture_leaderboard_og_ops_admin_index_path %>" method="post" data-controller="submit-form">
              <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
              <button type="submit" class="<%= ui_secondary_button_class %> px-4" data-submit-form-target="button">
                <span data-submit-form-target="spinner" class="hidden"><%= ui_spinner_icon(classes: "h-3.5 w-3.5") %></span>
                <span data-submit-form-target="text">Capture OG</span>
              </button>
            </form>
          </div>
        </div>

        <!-- GitHub App -->
        <div class="rounded border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-800">
          <h2 class="text-lg font-semibold mb-3">GitHub App Installation</h2>
          <div class="text-sm text-slate-600 dark:text-slate-300">
            <div>Configured installation_id: <span class="font-mono"><%= @configured_installation_id || 'nil' %></span></div>
          </div>
        </div>

        <!-- Email Smoke Test -->
        <div class="rounded border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-800">
          <h2 class="mb-3 text-lg font-semibold text-slate-900 dark:text-slate-100">Email Smoke Test</h2>
          <%= form_with url: ops_send_test_email_path, method: :post, local: true, html: { class: "space-y-4", data: { controller: "submit-form" } } do |f| %>
            <div class="space-y-1.5">
              <label class="text-sm font-medium text-slate-700 dark:text-slate-300">To (email)</label>
              <%= f.email_field :to, class: ui_input_class %>
            </div>
            <div class="space-y-1.5">
              <label class="text-sm font-medium text-slate-700 dark:text-slate-300">Message (optional)</label>
              <%= f.text_field :message, class: ui_input_class %>
            </div>
            <button type="submit" class="<%= ui_primary_button_class %>" data-submit-form-target="button">
              <span data-submit-form-target="spinner" class="hidden"><%= ui_spinner_icon %></span>
              <span data-submit-form-target="text">Send test email</span>
            </button>
          <% end %>
        </div>

        <% if @dev_log_tail.present? %>
          <!-- Development Log -->
          <div class="rounded border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-800">
            <h2 class="text-lg font-semibold mb-3">Development Log (tail)</h2>
            <pre class="max-h-96 overflow-auto whitespace-pre-wrap text-xs"><%= @dev_log_tail %></pre>
          </div>
        <% end %>
      </div>

      <!-- Jobs Tab -->
      <div data-tabs-target="panel" data-tab-panel="jobs" class="hidden space-y-6">
        <!-- Quick Ownership -->
        <div class="rounded border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-800">
          <h2 class="text-lg font-semibold mb-3">Quick Ownership</h2>
          <p class="mb-3 text-sm text-slate-600 dark:text-slate-300">Transfer ownership by GitHub login. Profiles have exactly one owner. Set Owner is mainly for legacy orphaned profiles.</p>
          <div class="grid gap-4 sm:grid-cols-2">
            <div class="rounded border border-slate-200 p-3 dark:border-slate-700">
              <h3 class="text-sm font-semibold mb-2">Set Owner (orphaned only)</h3>
              <form action="<%= ops_set_owner_ownership_path %>" method="post" class="space-y-3" data-controller="submit-form">
                <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
                <div data-controller="ops-autocomplete" data-ops-autocomplete-endpoint-value="/ops/profiles/search" data-action="click@window->ops-autocomplete#hide" class="relative space-y-1.5">
                  <label class="text-xs font-semibold uppercase tracking-wide text-slate-500 dark:text-slate-400">Profile login</label>
                  <input type="text" name="profile_login" placeholder="@login" class="<%= ui_input_class %>" data-ops-autocomplete-target="input" data-action="input->ops-autocomplete#search" />
                  <div data-ops-autocomplete-target="results" class="hidden absolute z-50 mt-1 w-full rounded border border-slate-200 bg-white shadow dark:border-slate-700 dark:bg-slate-800"></div>
                </div>
                <div data-controller="ops-autocomplete" data-ops-autocomplete-endpoint-value="/ops/users/search" data-action="click@window->ops-autocomplete#hide" class="relative space-y-1.5">
                  <label class="text-xs font-semibold uppercase tracking-wide text-slate-500 dark:text-slate-400">Target user login</label>
                  <input type="text" name="target_login" placeholder="@user" class="<%= ui_input_class %>" data-ops-autocomplete-target="input" data-action="input->ops-autocomplete#search" />
                  <div data-ops-autocomplete-target="results" class="hidden absolute z-50 mt-1 w-full rounded border border-slate-200 bg-white shadow dark:border-slate-700 dark:bg-slate-800"></div>
                </div>
                <button type="submit" class="<%= ui_primary_button_class %> w-full" data-submit-form-target="button">
                  <span data-submit-form-target="spinner" class="hidden"><%= ui_spinner_icon %></span>
                  <span data-submit-form-target="text">Set owner</span>
                </button>
              </form>
            </div>
            <div class="rounded border border-slate-200 p-3 dark:border-slate-700">
              <h3 class="text-sm font-semibold mb-2">Transfer Owner</h3>
              <form action="<%= ops_transfer_by_profile_ownership_path %>" method="post" class="space-y-3" data-controller="submit-form">
                <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
                <div data-controller="ops-autocomplete" data-ops-autocomplete-endpoint-value="/ops/profiles/search" data-action="click@window->ops-autocomplete#hide" class="relative space-y-1.5">
                  <label class="text-xs font-semibold uppercase tracking-wide text-slate-500 dark:text-slate-400">Profile login</label>
                  <input type="text" name="profile_login" placeholder="@login" class="<%= ui_input_class %>" data-ops-autocomplete-target="input" data-action="input->ops-autocomplete#search" />
                  <div data-ops-autocomplete-target="results" class="hidden absolute z-50 mt-1 w-full rounded border border-slate-200 bg-white shadow dark:border-slate-700 dark:bg-slate-800"></div>
                </div>
                <div data-controller="ops-autocomplete" data-ops-autocomplete-endpoint-value="/ops/users/search" data-action="click@window->ops-autocomplete#hide" class="relative space-y-1.5">
                  <label class="text-xs font-semibold uppercase tracking-wide text-slate-500 dark:text-slate-400">New owner (user login)</label>
                  <input type="text" name="target_login" placeholder="@user" class="<%= ui_input_class %>" data-ops-autocomplete-target="input" data-action="input->ops-autocomplete#search" />
                  <div data-ops-autocomplete-target="results" class="hidden absolute z-50 mt-1 w-full rounded border border-slate-200 bg-white shadow dark:border-slate-700 dark:bg-slate-800"></div>
                </div>
                <button type="submit" class="<%= ui_secondary_button_class %> w-full" data-submit-form-target="button">
                  <span data-submit-form-target="spinner" class="hidden"><%= ui_spinner_icon %></span>
                  <span data-submit-form-target="text">Transfer owner</span>
                </button>
              </form>
            </div>
          </div>
        </div>

        <% if @engine_present %>
          <!-- Mission Control Info -->
          <div class="rounded border border-slate-200 bg-white p-4 text-slate-700 shadow-sm dark:border-slate-700 dark:bg-slate-800 dark:text-slate-200">
            Mission Control is installed. Open <a href="/ops/jobs" class="text-blue-600 dark:text-blue-400">/ops/jobs</a> for full jobs UI.
          </div>

          <!-- Jobs UI Iframe -->
          <div class="rounded border border-slate-200 bg-white p-0 shadow-sm dark:border-slate-700 dark:bg-slate-800">
            <div class="flex items-center justify-between border-b border-slate-200 px-4 py-3 text-slate-700 dark:border-slate-700 dark:text-slate-200">
              <h2 class="text-lg font-semibold">Jobs UI</h2>
              <a href="/ops/jobs" class="rounded px-2 py-1 text-blue-600 underline dark:text-blue-400" target="_blank" rel="noopener">Open full screen</a>
            </div>
            <div class="h-[75vh] w-full" style="height: 75vh;">
              <iframe id="ops-jobs-frame" src="/ops/jobs" title="Mission Control Jobs"
                      style="border:0; width:100%; height:100%; background:transparent"></iframe>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</section>
